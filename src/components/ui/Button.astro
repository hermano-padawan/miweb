---

import "../../styles/components/button.css";
import { withBase } from "../../utils/paths";


interface Props {
  href?: string;
  variant?: "primary" | "secondary" | "outline";
}

const {
  href,
  variant = "primary"
} = Astro.props;
---

{
  href ? (
    <a href={withBase(href)} class={`button button--${variant}`}>
      <slot />
    </a>
  ) : (
    <button class={`button button--${variant}`}>
      <slot />
    </button>
  )
}
